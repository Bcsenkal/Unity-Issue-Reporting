{
  "name": "issue-report-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker.test.mjs",
    "check": "wrangler deploy --dry-run --config wrangler.example.jsonc",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
